# When flutter version is updated, you should update this version.
include: package:yumemi_lints/dart/3.2.2/recommended.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "tools/bricks/**"
