name: core_domain
description: This package provides use cases and domain models.
publish_to: "none"

environment:
  sdk: 3.2.4

dependencies:
  core_common:
    path: ../common
  core_data:
    path: ../data
  core_model:
    path: ../model

dev_dependencies:
  core_testing:
    path: ../testing
  test: 1.24.0
  yumemi_lints: 1.3.0
